<% if user_signed_in? %>
<h1>Editing Patient</h1>

<%= render 'form', patient: @patient %>

<%= link_to 'Show', @patient %> |
<%= link_to 'Back', patients_path %>
<% else %>
This page is not available to users who are not signed in. 
<% end %>
